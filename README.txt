The files with "test" in their name need to be put into a package called com.tests in the tests folder.

The files ReadDisc, RenameFile, SearchDisc need to be put into a package called com.tests in the src folder.

The ZIP file is there just in case the files don't work alone as the full project will work correctly.